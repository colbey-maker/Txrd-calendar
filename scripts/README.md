# Scripts

This folder contains the tools that generate the Apple Calendar.

Eventually this folder will include:

- Build the practice calendar
- Build the bout calendar
- Build the goals calendar
- Combine them into one subscription calendar

The source of truth for the project is the files in `/data`.

The generated calendars will be written to `/dist`.
